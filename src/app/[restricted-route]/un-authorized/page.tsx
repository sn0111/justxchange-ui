'use client';
import UnauthorizedPage from '@/components/UnAuthorized';
import React from 'react';

const page = () => {
  return (
    <div>
      <UnauthorizedPage />
    </div>
  );
};

export default page;
